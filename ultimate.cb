

struct Reg{
  Int x, y;
  Double a, b;
};

Array<10, Reg> arrei;

Pointer<Array<10, Reg>> p := addr(arrei);


func Int foo(Int x, Reg r){
  if(1 + 1 == 2 + 2) then {
    x := 1 + 3 * 5 ^ 8 > 0 + cast(10 ^ x, Double);
  }else{
    x := substr("eee", x, x + 5);
  };
};

proc fee(Reg x, Pointer<Reg> p){
  read(*x);
  p := cast(p, Reg);
  *x := p;
  x := addr(p);

  free x;
};



Double y := 5 ^ (10 + (3 + (7 % 1 - 3) ^ -1) + x);
y := (1 ^ ((1) ^ 1) ^ 1) ^ 1;
